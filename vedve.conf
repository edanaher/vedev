name = "hi there me"

f = function()
  print("hi from function")
  return "THE RETURN VALUE"
end

keys = {
  [17] = function(event) send_event(event.type, event.code, event.value) return -2 end,
  [18] = function(event) return 18 end,
  [65] = function(event) send_event(event.type, 17, event.value) schedule(10.1, function() send_event(event.type, 18, 1) send_event(event.type, 18, 0) end) return -2 end,
  [64] = function(event)
    print("Lua for event", event.value)
    send_event(event.type, 17, event.value)
    schedule(2000, function()
                     print("Triggering callback from", event.value)
                     send_event(event.type, 18, 1)
                     send_event(event.type, 18, 0)
                   end)
    print("Scheduled for", event.value)
    return -2 end
}
